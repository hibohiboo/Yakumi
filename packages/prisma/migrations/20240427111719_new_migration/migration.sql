BEGIN TRY

BEGIN TRAN;

-- AlterTable
ALTER TABLE [dbo].[YakumiCharacter] ALTER COLUMN [data] NVARCHAR(max) NOT NULL;

COMMIT TRAN;

END TRY
BEGIN CATCH

IF @@TRANCOUNT > 0
BEGIN
    ROLLBACK TRAN;
END;
THROW

END CATCH
