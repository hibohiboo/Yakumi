{
  "name": "@yakumi/batch-functions",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w",
    "clean": "rimraf dist",
    "prestart": "npm run clean && npm run build",
    "start": "func start",
    "test": "echo \"No tests yet...\"",
    "ncu": "ncu -u",
    "deploy": "dotenv -e .env -- bash -c 'bash deploy.sh'"
  },
  "dependencies": {
    "@azure/functions": "^4.6.0"
  },
  "devDependencies": {
    "@types/node": "^22.x",
    "dotenv-cli": "^8.0.0",
    "rimraf": "^6.0.1",
    "typescript": "^5.7.2"
  },
  "main": "dist/src/{index.js,functions/*.js}"
}
